# Audit — 2026-06-16

## High Priority
1. **README.md:128** — claims `.docx` needs `pandoc` and `.pdf` needs `pdftotext` (stale; code uses pure-Python `python-docx`/`pypdf`). Fix wording.
2. **Missing `.gitignore`** — `venv/`, `__pycache__/`, `.pytest_cache/`, `.ruff_cache/` would be staged by `git add .`. Add standard Python ignores.
3. **Stale `__pycache__/handoff.cpython-314.pyc`** and `tests/__pycache__/test_handoff*` — from deleted module (v0.3.0 handoff removal). Remove.

## Medium Priority
4. **Dead `retries` param in `generator.py:77`** — `call_llm(prompt, *, retries=1)` accepts `retries` but never forwards it to the provider's `generate()`. Either wire it through or remove it from the signature.
5. **Test coverage ~50%** — untested areas:
   - `profile.py`: `build_voice_fingerprint()` full computation
   - `providers/config.py`: `ProviderConfig` load/save roundtrip, `resolve_key`
   - `providers/detect.py`, `providers/registry.py`
   - `generator.py`: `_extract_json`, `extract_contact_fields`, `extract_job_fields`
   - `app.py` routes: `/refine`, `/extract-contact`, `/extract-job`, `/import-org`, `/profile-status`, `/providers/select`, `/providers/key`, `/providers/models`
   - `docx_writer.py`: edge cases (no salutation, no closing, malformed input)
6. **Stale docs/** references to deleted handoff feature in `docs/provider-roadmap.md` and `docs/platform-guide-implementation.md`.

## Low Priority
7. **No CSRF protection** — fine for localhost-only, but flaggable if `JALLM_HOST=0.0.0.0` is ever used.
8. **No rate limiting** on LLM-calling endpoints — rapid UI clicks could spawn many concurrent calls.
9. **`_SENTENCE_SPLIT` regex** in `profile.py:100` misses sentences ending in `."` or `)` and may split on abbreviations like "U.S. Army". Acceptable for voice-fingerprint heuristic.
10. **`_PROVIDER_CONFIG` singleton** never reloads from disk if config file is edited externally while server is running. Fine for single-user.
11. Consider adding `conftest.py` with shared `SAMPLE_PROFILE`/`JOB` fixtures to reduce test duplication.
